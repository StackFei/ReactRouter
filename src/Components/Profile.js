import React,{Component} from 'react';
import {HashRouter as Router,Route} from 'react-router-dom'
class Propfile extends Component {
  constructor(props){
    super(props)
  }
  render(){
    return (<div>
    		用户中心
    	</div>);
  }
}

export default Propfile;
